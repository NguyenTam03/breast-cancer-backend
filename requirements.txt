# ---- web/api ----
fastapi==0.115.0
uvicorn[standard]==0.30.0
motor==3.6.0
beanie==1.27.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
pydantic[email]==2.9.0
pydantic-settings==2.5.2
httpx==0.27.0
aiofiles==24.1.0
email-validator==2.2.0
python-dotenv==1.0.1

# ---- ML / CV ----
numpy==1.26.4              # ổn cho TF 2.20 + scikit-learn 1.5
opencv-python==4.10.0.84
Pillow==10.4.0
scikit-learn==1.5.1

# ---- TensorFlow (CPU) ----
tensorflow-cpu==2.20.0
protobuf==4.25.3           # tránh kéo bản 5.x gây xung đột
h5py>=3.10,<4              # tránh bản quá cũ
# KHÔNG thêm 'keras' riêng -> dùng tf.keras
# KHÔNG thêm tensorflow-io trừ khi bạn thật sự cần
